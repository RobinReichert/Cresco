# logic
 
This crate contains the shared business logic of the project.
It is kept separate from the `cross` crate so that it can be
compiled and tested natively on the host machine without
requiring embedded toolchains or hardware.
